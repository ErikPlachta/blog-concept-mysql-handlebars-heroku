<section class="card">
  
    <!-- The card contains the post name as the card header -->
    <banner>Posted by u/{{user.username}} on {{format_date created_date}}</banner>
    <header>
      <h4>{{title}} by {{user.username}}</h4>
    </header>
    
    {{!-- <span>{{content}}</span> --}}
    
    {{!-- <img src="{{resource.url}}" alt="{{resource.title}}"> --}}

    <div>
      <span>Post ID: {{id}}</span>
      <span>Last Modified: {{format_date modified_date}}</span>
    </div>
    
  </a>
</section>
