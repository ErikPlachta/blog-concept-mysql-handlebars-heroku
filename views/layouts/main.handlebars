<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Erik Plachta | Tech Blog - Concept App</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Tech Blog - Concept App</h1>
  </header>
  <nav>
    <a><a href="/">Home</a></button>
    <!-- If the user is logged in, show a logout button -->
    {{#if loggedIn}}
      <a id="dashboard" class="btn-no-style">Dashboard</a>
      <a id="logout" class="btn-no-style">Logout</a>
    {{else}}
    <!-- Otherwise, show a link to login -->
      <a href="/login">Login</a>
    {{/if}}
    
    <a href="https://github.com/ErikPlachta/blog-app-cloudhosted-mvc-modular" target="_blank">GitHub</a>
  </nav>
  <main>

    {{!-- left bar --}}
    <aside>
      <section class="activity-feed">
        <h3>Activity Feed</h3>
        <hr>
        <h4>Posts</h4>
        <div class="popular-posts">
          {{#each posts as |post|}}
            {{> post-stats}}
          {{/each}}
        </div>
        <div class="popular-comments">
          <h4>Comments</h4>
          {{#each comments as |comment|}}
            {{> comment-stats}}
          {{/each}}
        </div>
      </section>
      {{!-- posts --}}
       <section class="post-stats">
        <h3>Post Stats</h3>
        <hr>
        <h4>TBD</h4>
      </section>
    </aside>
    
    {{!-- Main area --}}
    <div class="landing">
      {{{body}}}
    </div>
  </main>
  
  <!-- If the user is logged in, load the JS script for if clicked logout btn -->
  {{#if loggedIn}}
    <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
